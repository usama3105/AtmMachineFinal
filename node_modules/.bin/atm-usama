#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/ts-node" ]; then
  exec "$basedir/ts-node"  "$basedir/../atm-usama/main.js" "$@"
else 
  exec ts-node  "$basedir/../atm-usama/main.js" "$@"
fi
